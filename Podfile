# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RegistrationTestTask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RegistrationTestTask
  
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'  
  pod 'Alamofire', '~> 4.4'
  
  target 'RegistrationTestTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RegistrationTestTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
